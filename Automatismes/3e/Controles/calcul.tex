\if \thechoixa1  \it
  Calculer  $12 -15$ 
\fi   
\if \thechoixa2 \it
  Calculer  $-12 - 10$ 
\fi   \if\thechoixa3  \it
  Calculer  $-10+ 12$ 
\fi   \if\thechoixa4  \it
  Calculer  $-10-5$ 

\fi   \if\thechoixb1  \it
  Calculer  $19-8$
\fi   \if\thechoixb2  \it
  Calculer  $-9 - 15$
\fi   \if\thechoixb3  \it
  Calculer  $-16 + 9$
\fi   \if\thechoixb4 \it
  Calculer  $-9 + (-16)$

\fi   \if\thechoixc1  \it
  Calculer  $-4 - (-2)$
\fi   \if\thechoixc2  \it
  Calculer  $5 - (-3)$
\fi   \if\thechoixc3  \it
  Calculer  $-(-3) + 5$
\fi   \if\thechoixc4  \it
  Calculer  $-(-1) - (-3)$
\fi   \if\thechoixc5  \it
  Calculer  $-(-6) - 5$

\fi   \if\thechoixd1  \it
  Calculer  $5 + 4 - 8 - 1 + 4 -2 $
\fi   \if\thechoixd2  \it
  Calculer  $-3 - 5 - 1 + 9$
\fi   \if\thechoixd3  \it
  Calculer  $17 - 12 + 13 - 18$

\fi   \if\thechoixe1  \it
  Calculer  $14- (5-2 - (4-9))$
\fi   \if\thechoixe2  \it
  Calculer  $-(12-15) + (3-4) - (5-(-9))$ 
\fi   \if\thechoixe3  \it
  Calculer  $-(9-14) - (3-21) - (4-(-10))$ 

\fi   \if\thechoixf1  \it
  Calculer  $12 \times -3$
\fi   \if\thechoixf2  \it
  Calculer  $-3 \times 4 $
\fi   \if\thechoixf3  \it
  Calculer  $-5 \times -2$ 
\fi   \if\thechoixf4  \it
  Calculer  $8\div(-4)$
\fi   \if\thechoixf5  \it
  Calculer  $-9\div3$
\fi   \if\thechoixf6  \it
  Calculer  $(-15)\div(-5)$
\fi   \if\thechoixf7  \it
  Calculer  $18\div(-2)$
\fi   \if\thechoixf8 \it
  Calculer  $-45\div5$
\fi   \if\thechoixf9  \it
  Calculer  $(-119)\div(-7)$

\fi   \if\thechoixg1  \it
  Calculer  $3\times 4 - 5$
\fi   \if\thechoixg2  \it
  Calculer  $-3 \times -2 + 6 \times -4$
\fi   \if\thechoixg3  \it
  Calculer  $-4 - 5 \times 2$
\fi   \if\thechoixg4  \it
  Calculer  $4 - 10 \div (-2)$
\fi   \if\thechoixg5  \it
  Calculer  $3\times (-4) - (-4) \div 2$
\fi   \if\thechoixg6  \it
  Calculer  $(15 \div (-3) - 2)\times (5- 8)$

\fi   \if\thechoixh1  \it
  Calculer  $\displaystyle\frac34 + \frac14$
\fi   \if\thechoixh2  \it
  Calculer  $\displaystyle\frac32 + \frac52 + \frac72$
\fi   \if\thechoixh3  \it
  Calculer  $\displaystyle-\frac13 + \frac23 + \frac53$
\fi   \if\thechoixh4  \it
  Calculer  $\displaystyle\frac49 - \frac29 - \frac{-4}9$
\fi   \if\thechoixh5  \it
  Calculer  $\displaystyle\frac23 - \frac{10}3 - \frac{1}3$
\fi   \if\thechoixh6  \it
  Calculer  $\displaystyle\frac12 + \frac13$
\fi   \if\thechoixh7  \it
  Calculer  $\displaystyle-\frac14 + \frac12$
\fi   \if\thechoixh8  \it
  Calculer  $\displaystyle\frac1{10} - \frac35$
\fi   \if\thechoixh9  \it
  Calculer  $\displaystyle\frac2{10} - \frac12 - \frac15$ 

\fi   \if\thechoixi1  \it
  Calculer  $\displaystyle\frac34 \times \frac14$
\fi   \if\thechoixi2 \it
  Calculer  $\displaystyle\frac32 \times\frac72$
\fi   \if\thechoixi3 \it
  Calculer  $\displaystyle-\frac13 \times \frac23 $

\fi   \if\thechoixj2  \it
  Calculer  $\displaystyle\frac49 \div \frac29 $
\fi   \if\thechoixj1  \it
  Calculer  $\displaystyle\frac23 \div \frac54$

\fi   \if\thechoixk1  \it
  Calculer  $\displaystyle\frac12 \times \frac13\div \frac14$
\fi   \if\thechoixk2  \it
  Calculer  $\displaystyle-\frac14 \div  \frac12 \times \frac35$
\fi   \if\thechoixk3  \it
  Calculer  $\displaystyle\frac1{10} - \frac12\times \frac35$
\fi   \if\thechoixk4  \it
  Calculer  $\displaystyle\frac2{10}\div\frac15 - \frac12 \times \frac15$
\fi   \if\thechoixk5  \it
  Calculer  $\displaystyle\frac2{15}\div 3 - \frac3{10} \times - \frac16$

\fi   \if\thechoixl1  \it
  Calculer  $\displaystyle3\times \frac{-3}4$
\fi   \if\thechoixl2  \it
  Calculer  $\displaystyle2\div \frac13$
\fi   \if\thechoixl3  \it
  Calculer  $\displaystyle\frac16\div2$
\fi   \if\thechoixl4  \it
  Calculer  $\displaystyle\frac{\frac12}{\frac13}$

\fi   \if\thechoixm1  \it
  Calculer  $\displaystyle\frac{7}{\frac58}$
\fi   \if\thechoixm2  \it
  Calculer  $\displaystyle\frac{\frac65}{-12}$

\fi   \if\thechoixn1 \it
  Calculer  $2^3$
\fi   \if\thechoixn2  \it
  Calculer  $3^2$
\fi   \if\thechoixn3  \it
  Calculer  $5^3$
\fi   \if\thechoixn4  \it
  Calculer  $2^6$ 
\fi   \if\thechoixn5  \it
  Calculer  $8^2$

\fi   \if\thechoixo1 \it
  Calculer  $(-3)^2$
\fi   \if\thechoixo2  \it
  Calculer  $(-4)^3$
\fi   \if\thechoixo3  \it
  Calculer  $(-2)^3\div3^2$

 \fi   \if\thechoixp1 \it
  Calculer  $\left(\frac13\right)^3$
\fi   \if\thechoixp2  \it
  Calculer  $\left(\frac34\right)^3$
\fi   \if\thechoixp3  \it
  Calculer  $\left(\frac45\right)^2$
 \fi   \if\thechoixp4 \it
  Calculer  $\left(\frac67\right)^3$
 \fi   \if\thechoixp5 \it
  Calculer  $\left(\frac92\right)^2$
 
 \fi   \if\thechoixq1 \it
  Calculer  $3^{-3}$
\fi   \if\thechoixq2  \it
  Calculer  $4^{-2}$
\fi   \if\thechoixq3  \it
  Calculer  $5^{-3}$
\fi   \if\thechoixq4  \it
  Calculer  $2^{-6}$ 
\fi   \if\thechoixq5  \it
  Calculer  $10^{-10}$
\fi   \if\thechoixq6  \it
  Calculer  $(-8)^{-2}$ 
\fi   \if\thechoixq7  \it
  Calculer  $(-3)^{-3}$
\fi   \if\thechoixq8  \it
  Calculer  $(-4)^{-2}$

\fi   \if\thechoixr1  \it
  Calculer  $\left(\frac92\right)^{-2}$
\fi   \if\thechoixr2  \it
  Calculer  $\left(\frac13\right)^{-3}$
 \fi   \if\thechoixr3 \it
  Calculer  $\left(\frac34\right)^{-2}$
\fi   \if\thechoixr4  \it
  Calculer  $\left( \frac45 \right)^{-3}$
 \fi   \if\thechoixr5 \it
  Calculer  $\left(\frac67\right)^{-3}$
\fi
\if\thechoixs1  \it
  Écrire sous forme scientifique le nombre  $\np{1343,23}$
\fi   \if\thechoixs2  \it
  Écrire sous forme scientifique le nombre  $\np{103258202}$
\fi   \if\thechoixs3  \it
  Écrire sous forme scientifique le nombre  $\np{3298,204}$
\fi   \if\thechoixs4  \it
  Écrire sous forme scientifique le nombre  $\np{0,813429}$
 \fi   \if\thechoixs5 \it
  Écrire sous forme scientifique le nombre  $\np{954316372,135}$
 \fi   \if\thechoixs6 \it
  Écrire sous forme scientifique le nombre  $\np{0,00034254}$
\fi   \if\thechoixs7  \it
  Écrire sous forme scientifique le nombre  $\np{2350000}$
\fi   \if\thechoixs8  \it
  Écrire sous forme scientifique le nombre  $\np{2353,129}$
\fi   \if\thechoixs9  \it
  Écrire sous forme scientifique le nombre  $\np{9431425,86502335}$ 
\fi   \if\thechoixt1  \it
  Écrire sous forme scientifique le nombre  $2,34 \times 10^5 \times 4,12 \times 10^{-2}$
\fi   \if\thechoixt2  \it
  Écrire sous forme scientifique le nombre  $432,12 \times 10^{-1} \times 142,1 \times 10^{-9}$
\fi   \if\thechoixt3  \it
  Écrire sous forme scientifique le nombre  $10^{-1}+10^{-2}$
\fi   \if\thechoixt4  \it
  Écrire sous forme scientifique le nombre  $2\times 10^3 + 3 \times 10^1$
\fi
  
%\newpage